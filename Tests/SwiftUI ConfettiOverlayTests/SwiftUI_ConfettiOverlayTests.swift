import XCTest
@testable import SwiftUI_ConfettiOverlay

final class SwiftUI_ConfettiOverlayTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUI_ConfettiOverlay().text, "Hello, World!")
    }
}
